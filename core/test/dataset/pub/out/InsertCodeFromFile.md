Я: Можно мне читаемый фильтр для текста?

Regex: Да без проблем `:)`

Regex:

Текст регулярки
```txt [Текст регулярки]{0--1}
(?P<class>(class[\t ]+.+[\t ]*)(?:\((.+)\))?:\n(?:\t+| {1,4}.*\n+)+)|
(?P<func>(def[\t ]+.+[\t ]*)\((.+):\n(?:(?:\t+| {1,4}).*\n+)+)|
(?:\A|\n)\s*(?P<var>.+[\t ]*=.+)
```

Конец.